---
import './styles.css';
import './scripts.js';
import LazyImage from '../../common/LazyImage.astro';
import { logos } from '../../../config/assets';
---
<header class="pcc-header bg-white/80 backdrop-blur supports-[backdrop-filter]:bg-white/60 border-b border-gray-200 sticky top-0 z-[1000]">
  <div class="container mx-auto px-4 py-3 flex items-center justify-between">
    <a href="/productosconcausa" class="flex items-center gap-3" aria-label="Productos con Causa">
      <LazyImage src={logos.principal.url} alt={logos.principal.alt} class="h-8 w-auto" />
     
    </a>
    <nav class="hidden md:flex items-center gap-6 text-sm text-secondary">
      <a href="/productosconcausa" class="pcc-link">Inicio</a>
      <a href="/productosconcausa/historia" class="pcc-link">Historia</a>
    </nav>
    <button class="md:hidden pcc-menu-btn" aria-label="Abrir menú" aria-expanded="false">
      <span class="pcc-menu-icon"></span>
    </button>
  </div>
  <div class="pcc-mobile-menu md:hidden" hidden>
    <nav class="container mx-auto px-4 py-4 flex flex-col gap-4 text-base text-secondary">
      <a href="/productosconcausa" class="pcc-link">Inicio</a>
      <a href="/productosconcausa/historia" class="pcc-link">Historia</a>
    </nav>
  </div>
</header>
